'use client'
const Loading = () => {

    return (
        <div className="flex items-center justify-center h-full">
        <p>loading ..............</p>
    </div>
    );
};

export default Loading;